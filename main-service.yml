server:
  port: 8083

spring:
  application:
    name: main-service

  datasource:
    url: jdbc:mysql://localhost:3306/main_service_db
    username: springuser
    password: spring123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
